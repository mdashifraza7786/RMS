# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=rms

# Note: Create a .env file with these variables in your project root
# Next.js will automatically load them - no need to import dotenv/config